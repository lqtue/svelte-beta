import { FetchFn } from '@allmaps/types';

declare const fetchAndGetImageBitmapWorker: {
    getImageBitmap(tileUrl: string, abortControllerSignal: AbortSignal, fetchFn: FetchFn | undefined, width: number, height: number): Promise<ImageBitmap>;
};
export type FetchAndGetImageBitmapWorkerType = typeof fetchAndGetImageBitmapWorker;
export {};
