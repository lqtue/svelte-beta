.DS_Store
node_modules
/build
/.svelte-kit
/.wrangler
__pycache__/
*.pyc
.env
.env.*
!.env.example
# local Supabase demo keys only — see the header in the file
!.env.test
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright
/test-results
/playwright-report
/blob-report

# SAM2 model checkpoints
*.pt
*.pth

.tile_cache/

# OCR pipeline outputs (local artifacts)
work/ocr/outputs/
work/ocr/.venv/

# Claude Code local session state
.claude/
worker/.wrangler

# PMTiles basemap extracts are built by scripts/pmtiles_extract.sh and live in R2, never in git
*.pmtiles
work/cleanup/
